server:
  port: 8080
  servlet:
    context-path: /account_service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/FoodApp
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

key:
  value: this-is-a-very-long-secret-key-32-characters

app:
  services:
    cart: http://localhost:8081/order_service/cart
